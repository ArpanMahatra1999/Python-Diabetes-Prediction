Use following commands to run app:
1. pip install -r requirements.txt
create heroku app and deploy
copy url from heroku app to api_implementation's url
let the local running in terminal
open next local in terminal
3. python api_implementation.py